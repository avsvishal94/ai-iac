version: "3.9"

services:
  vision-ai:
    build: ../services/vision-ai
    ports:
      - "8001:8000"

  api-gateway:
    build: ../apps/api-gateway
    ports:
      - "8000:8000"
    depends_on:
      - vision-ai
